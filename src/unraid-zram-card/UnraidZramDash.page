Menu="Dashboard:0"
Icon="server"
---
<?php
try {
    $docroot = $docroot ?? $_SERVER['DOCUMENT_ROOT'] ?: '/usr/local/emhttp';
    $includeFile = "{$docroot}/plugins/unraid-zram-card/ZramCard.php";

    if (file_exists($includeFile)) {
        require_once $includeFile;
        if (function_exists('getZramDashboardCard')) {
            $mytiles['unraid-zram-card']['column2'] = getZramDashboardCard();
        }
    }
} catch (Throwable $e) {
    $mytiles['unraid-zram-card']['column2'] = '<tbody title="Unraid ZRAM Card"><tr><td>Error: ' . htmlspecialchars($e->getMessage()) . '</td></tr></tbody>';
}
?>
